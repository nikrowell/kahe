{
  "name": "@nikrowell/turbine",
  "version": "0.6.0",
  "description": "",
  "author": "Nik Rowell <nik@nikrowell.com>",
  "license": "MIT",
  "main": "turbine.js",
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/nikrowell/turbine.git"
  },
  "keywords": [
    "browser",
    "pushstate",
    "router",
    "frontend",
    "framework",
    "transitions"
  ],
  "scripts": {
    "start": "rollup -c -m -o turbine.js --watch",
    "bundle": "rollup -c -m -o turbine.js",
    "build": "rollup -c | uglifyjs -mc --comments /^!/ > turbine.min.js",
    "test": "budo test:bundle.js --live --dir test --title turbine -- -t [ babelify --presets [ es2015 ] ] --plugin tap-dev-tool",
    "version": "npm run bundle && npm run build && git add --all"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "rollup": "^0.43.0",
    "rollup-plugin-buble": "^0.15.0",
    "rollup-watch": "^4.0.0",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.7.0",
    "uglify-js": "^2.8.22"
  }
}
